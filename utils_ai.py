from typing import Any, Dict, List


def parse_financial_text(text: str) -> List[Dict[str, Any]]:
    """
    Placeholder AI parser for financial text.

    Returns an empty list when no parser is configured.
    """
    if not text:
        return []
    return []
